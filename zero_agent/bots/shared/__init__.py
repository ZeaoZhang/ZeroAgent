"""Shared command modules for ZeroAgent bots.

continue_cmd — /continue session restore
btw_cmd      — /btw side question
review_cmd   — /review code review
export_cmd   — /export file saving
session_names — persistent session naming
"""
